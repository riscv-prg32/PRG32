# Dual Playfield Demo

This demo draws two tile playfields:

- layer 0: an opaque checker background
- layer 1: transparent foreground platforms

The layers scroll at different speeds so students can see that both playfields
are independent.

Use entry prefix `dual_playfield` for the assembly demo and `dual_playfield_c`
for the C demo.
