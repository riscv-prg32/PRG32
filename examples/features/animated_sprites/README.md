# Animated Sprites Demo

This demo draws a tiny two-frame sprite sheet using the animation helpers:

- `prg32_sprite_anim_frame`
- `prg32_sprite_draw_frame`

The sprite moves horizontally and flips between two frames based on the current
tick count.

Use entry prefix `animated_sprites` for the assembly demo and
`animated_sprites_c` for the C demo.
