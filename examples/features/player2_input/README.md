# Player 2 Input Demo

This demo shows the shared PRG32 input bitmask for two players. Player 1 uses
the normal button bits. Player 2 uses the shifted `PRG32_P2_*` bits or the
normalized `prg32_input_read_player(2)` helper.

Use entry prefix `player2_input_c`.
