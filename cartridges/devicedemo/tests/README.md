# Tests

`check_source.sh` performs source/metadata sanity checks without requiring the cross toolchain. A real validation run should additionally build against PRG32 `development-c6` for ESP32-C6 and/or QEMU.
